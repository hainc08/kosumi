export declare function makeCode(prefix: string, seq: number, pad?: number): string;
export declare function nextQuoteCode(existingCodes: string[]): string;
export declare function addDays(iso: string, days: number): string;

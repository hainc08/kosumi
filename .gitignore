# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Dependency directories
node_modules/

# Build and dist
dist/
build/
.next/
.nuxt/
out/

# TypeScript cache
*.tsbuildinfo

# dotenv environment variables file
.env
.env.*
!.env.example
# Cấu hình build FE production (không chứa secret, được nhúng vào bundle) → vẫn track
!frontend/.env.production

# Frontend build được copy vào backend để serve chung 1 domain (build output)
backend/client/

# === Deploy artifact: bản build sẵn, TRACK để server git pull về chạy ngay ===
# (deploy/dist bị dính rule "dist/" ở trên nên phải mở lại; deploy/client thì không)
!deploy/dist/
!deploy/dist/**
# .env & node_modules trong deploy vẫn bị ignore (do rule chung) → đúng ý: không commit secret

# OS metadata
.DS_Store
Thumbs.db

# Playwright MCP artifacts
.playwright-mcp/

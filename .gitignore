# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Dependency directories
node_modules/

# Build and dist
dist/
build/
.next/
.nuxt/
out/

# TypeScript cache
*.tsbuildinfo

# dotenv environment variables file
.env
.env.*
!.env.example
# Cấu hình build FE production (không chứa secret, được nhúng vào bundle) → vẫn track
!frontend/.env.production

# Frontend build được copy vào backend để serve chung 1 domain (build output)
backend/client/

# OS metadata
.DS_Store
Thumbs.db

# Playwright MCP artifacts
.playwright-mcp/
